<div class="card station">
	<section class="identification">
    	<span class="card_no">{{ $station->id }}</span>
    	<span class="name">{{ $station->name }}</span>
    </section>
    <section class="points">
    	<span class="points">{{ $station->points }}</span>
    </section>
</div>